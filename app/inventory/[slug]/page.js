import Link from 'next/link'
import { ArrowLeft, ArrowRight, Check, Gauge, Fuel, Settings2 } from 'lucide-react'
import { cars } from '@/lib/cars'

export default async function CarDetail({ params }) { const { slug } = await params; const car = cars.find((item) => item.slug === slug) || cars[0]; return <main className="min-h-screen pb-24 pt-28"><div className="container"><Link href="/inventory" className="inline-flex items-center gap-2 text-sm font-bold text-primary"><ArrowLeft className="h-4 w-4" /> Back to inventory</Link><div className="mt-10 grid gap-10 lg:grid-cols-[1.2fr_.8fr] lg:items-start"><div><img src={car.images[0]} alt={car.name} className="aspect-[4/3] w-full rounded-3xl object-cover" /><div className="mt-8 grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl border border-border bg-card p-4"><Gauge className="mb-3 h-5 w-5 text-primary" />{car.mileage}</div><div className="rounded-xl border border-border bg-card p-4"><Settings2 className="mb-3 h-5 w-5 text-primary" />{car.transmission}</div><div className="rounded-xl border border-border bg-card p-4"><Fuel className="mb-3 h-5 w-5 text-primary" />{car.fuelType}</div><div className="rounded-xl border border-border bg-card p-4"><Check className="mb-3 h-5 w-5 text-primary" />Inspected</div></div></div><div className="lg:sticky lg:top-28"><p className="eyebrow">{car.year} · {car.bodyType}</p><h1 className="mt-3 font-display text-5xl font-bold">{car.name}</h1><p className="mt-4 font-display text-3xl font-bold text-primary">{car.price}</p><p className="mt-7 leading-7 text-muted-foreground">{car.description}</p><div className="mt-8 rounded-2xl border border-primary/30 bg-primary/10 p-6"><h2 className="font-display text-2xl font-bold">Interested in this car?</h2><p className="mt-2 text-sm text-muted-foreground">Ask about availability, financing, or book a test drive.</p><Link href="/contact" className="mt-5 inline-flex items-center gap-2 rounded-full bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">Inquire now <ArrowRight className="h-4 w-4" /></Link></div></div></div></div></main> }